# The retrieval API base URL (the Cloud Run service from deploy/cloudrun.md).
# For local dev against a locally-running API:
PUBLIC_API_URL=http://localhost:8080
# For production, set this in Vercel's project env to your Cloud Run URL, e.g.
# PUBLIC_API_URL=https://search-ranking-api-xxxxx-uc.a.run.app
