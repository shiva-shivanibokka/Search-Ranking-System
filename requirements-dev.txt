# Dev / CI dependencies.
# Installs the training stack (root requirements) plus the web-service runtime
# deps needed to *import* the FastAPI services during tests, plus lint/coverage.
# Used by .github/workflows/ci.yml and for local test runs.

-r requirements.txt

# ── Web service runtime (needed to import services.* in tests) ─────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
sqlalchemy==2.0.30
redis==5.0.4
structlog==24.1.0
prometheus-client==0.20.0

# ── Migrations ─────────────────────────────────────────────────────────────────
alembic==1.13.1

# ── Lint / coverage ────────────────────────────────────────────────────────────
ruff==0.15.12
pytest-cov==5.0.0
